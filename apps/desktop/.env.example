VITE_API_BASE_URL=/api
VITE_SSO_API_PREFIX=
VITE_OUTPUT_TARGET=
OUTPUT_TARGET=
# Release-build inputs consumed by scripts/write-updater-config.mjs.
# HTTPS only; never expose these as VITE_ values or runtime user input.
AW_DESKTOP_UPDATER_ENDPOINT=
AW_DESKTOP_UPDATER_PUBLIC_KEY=
